# claude-fleet

Persistent parcel-dispatch queue and async daemon that runs unattended `claude -p` sessions in parallel git worktrees, with dependency resolution, lease/retry mechanics, and optional auto-land on PASS verdicts.

> Status: bootstrapping — under active extraction. See `docs/` once published.
